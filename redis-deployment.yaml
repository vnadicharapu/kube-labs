apiVersion: v1
kind: Deployment
metadata: 
  name: redis-deployment
  labels:
    app: redisdemo
spec:
  relicas: 2
  selector:
    matchLabels:
      app: redis
   template:
      metadata:
        labels:
          app: redis
